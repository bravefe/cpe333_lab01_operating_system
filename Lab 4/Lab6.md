## 1. Understand the concept of virtual memory 
### 1.1 Explain and compare how virtual memory is implemented in modern Linux and MS Windows? 
Virtual memory is a memory management technique that gives each process its own virtual address space. When RAM does not have enough space to keep all required pages in memory, the operating system can move some pages to secondary storage, such as a disk or SSD, which is much slower than RAM. The operating system and CPU translate virtual addresses into physical RAM addresses using page tables.

### 1.2 Show step-by-step how to increase or decrease swap space in Linux withscreenshots. 
 
## 2. Understand the Linux VM Monitoring 
### 2.1 Run “free” command in the Linux shell 

### 2.2 Capture the output and explain everything that is shown by this command 

### 2.3 Summarize all numerical values shown by this command into a paragraph. 

### 2.4 Use  “free  --help”  and  select  2  useful  arguments  of  the  free  command  
could  be combination of multiple arguments if it is interesting. For each argument: 
- Describe what it does. 
- Show a sample command and explain the output. 
 
### 2.5 if you do not have “vmstat”, install and run it in the Linux shell. 

### 2.6 Capture the output and explain everything that is shown by this command 

### 2.7 Summarize all numerical values shown by this command into a paragraph. 

### 2.8 Use “vmstat --help” and select 2 useful arguments of the vmstat command 
could be combination of multiple arguments if it is interesting. For each argument: 
- Describe what it does. 
- Show a sample command and explain the output. 
 
## 3. Understand Windows VM Monitoring 
### 3.1 Open “Resource Monitor” 

### 3.2  Capture  the  output  and  explain  the  information  shown  on  the  interface  related  to memory monitoring. 

### 3.3 Summarize all numerical values shown in “physical memory” tab into a paragraph. 
 
 
## Submission 
 
Submit  the  document  files  into  LEB2  before  due  date.  This  is  the  problem  session  for  the 
group of up to 6 students. 
